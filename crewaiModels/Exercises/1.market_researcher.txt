# Ref: https://www.youtube.com/watch?v=tnejrr-0a94
# Last amended: 5th Sep. 2026

# Build market research agent that performs 
# market research using LLM only.



# ============
# AA. Activate crewai_env
# ============

# 1.0.1

-- If using VSCode

    cd ~/
    source /home/ashok/crewai_pjt/.venv/bin/activate
        (OR . activate_crewai_env.sh )

    When VScode is open: 
                     i)   Open your .py file
                     ii)  click ctrl+shift+p to open Python: Select Interpreter
                     iii)  Python: Select Interpreter
                     iv)   Look for crewai-pjt python environment.


# 1.0.2  START HERE

-- If using console

        cd /home/$USER/crewai_pjt
        rm -rf market_research         
        crewai create crew market_research
        # crewai create crew market_research --classic    


#==========
# BB. Agents
#==========

# 2.0.1 

name:		researcher
role:		Researcher
goal:		Perform thorough research on AI
backstory:	You are a diligent researcher with a passion for uncovering hidden insights and providing comprehensive analysis on AI.


# 2.0.2

name:		blogWriter
role:		Blog Writer
goal:		Write a comprehensive blog post on the research findings about AI
backstory:	A skilled writer with a knack for translating complex research findings into engaging and accessible blog posts.",


#==========
# CC. Tasks
#==========

# 3.0.1

description:		Analyze the latest trends in AI.",
expected_output:	A summary report.",
agent:			    researcher

# 3.0.2

description:		Blog Writer Task
expected_output: 	A Blog
agent:			    blogWriter

# 3.0.3 
    Start editing .jsonc files

        cd market_research
        code .

# 3.0.4

Context Required?:
-----------------
    Do I need to specify 'context' for 'Blog Writer Task'
    When is a context needed to be specified for a task. 
    For example, there are two tasks, task1 and task2, 
    each with the respective agents: researcher and 'blogWriter'.
    Blog writer is to write the blog using task2 after researcher
    has done his work. We create two agents, attach tasks to each 
    but no context is specified to 'task2' task. The 'bogWriter' 
    does work and gives output.

Answer:
------
    In sequential execution, if you don't specify context on task2,
    CrewAI defaults to giving it the output of all previously 
    executed tasks anyway — so task2 (blogWriter) still receives
    task1's (researcher's) output automatically, since sequential
    tasks by default use prior tasks' outputs as context. That's 
    why it worked without explicit context.
    Explicit context is needed when you want to:
        a.    Control which specific prior task(s) output are passed (not all previous ones)
        b.    Use a non-sequential/parallel or hierarchical setup where default ordering context isn't guaranteed
        c.    Explicitly exclude a task's output from context (using context=[])


#====== Command Execution Completed ==========

#==========
# DD. Modify researcher.jsonc
#==========#
#
# After the command 'crewai create crew market_research' is 
# fully executed, we have .jsonc files--one for every Agent and Task.
# We may have to amend the Agent configuration file: 


#==========
# DD. Modify crew.jsonc
#==========#

# Add the following line (look for embedder) to avoid memory recall error:

"embedder": {"provider": "ollama", "config": { "model": "nomic-embed-text:latest"}},



#============
# DD. Run
#==========

# 4.0.1

    cd market_research
    crewai run



####################3



